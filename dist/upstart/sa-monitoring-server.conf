# Upstart: /etc/init/sa-monitoring-server.conf
description "SA Monitoring Server"

start on (local-filesystems and net-device-up IFACE=eth0)
stop on [!12345]

respawn

pre-start script
  test -x /usr/bin/sa-monitoring-server || { stop; exit 0; }
end script

exec /usr/bin/sa-monitoring-server
